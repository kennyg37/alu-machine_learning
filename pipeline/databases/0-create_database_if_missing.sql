-- connect to the mysql server 
-- once connected, use your credentials to log in
-- for example: mysql -u root -p
-- once logged in, run the following command to create the database

CREATE DATABASE IF NOT EXISTS db_0;
